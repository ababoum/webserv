> test YoupiBanane
> test cookies with PHP, Python, and Perl
> do the scale
> do the read of input in multiple reads?
> test to upload a file

execve fail if hhtml non execve before


============== Cases to investigate ===================

-> POST with an empty body doesn't create the file
-> POST with a long body is not received entirely (text, image, etc.)
-> read issues on (very) long request