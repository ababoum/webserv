> redirection http (already parsed, we just need to 
	include the mechanism in the response)


> Build the autoindex mechanism (@Louis)

> Implement the POST mechanism (all the current Responses are treating GET only)
	Files should be stored in www/uploads

> Implement the DELETE mechanism (all the current Responses are treating GET only)